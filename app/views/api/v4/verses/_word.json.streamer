json.object! do
  json.id word.id
  json.position word.position
  json.audio word.audio_url
  json.char_type word.char_type_name

  json.translation do
    json.object! do
      json.extract! word.word_translation, :text, :language_name
    end
  end
end