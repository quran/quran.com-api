json.object! do
  json.audio_files do
    json.array! @presenter.audio_files do |audio_file|
      json.partial! 'audio_file', audio_file: audio_file, recitation: @presenter.recitation
    end
  end
end
