json.object! do
  json.changes do
    json.array! @episodes do |episode|
      json.object! do
        json.extract! episode,
                      :id,
                      :audio_url,
                      :reciter_name,
                      :reciter_photo,
                      :bio,
                      :chapter_name

      end
    end
  end
end
